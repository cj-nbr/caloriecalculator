---
import MedicalNote from "../../../components/ui/MedicalNote.astro";
import ContentLayout from "../../../layouts/ContentLayout.astro";
import ExerciseArticle from "../../../components/workouts/ExerciseArticle.astro";
import { RELATED_EXERCISE_TOPICS } from "../../../data/workout-topics";

const topic = RELATED_EXERCISE_TOPICS.find((item) => item.slug === "lying-hamstring-curl")!;
---

<ContentLayout
  title={`${topic.title} — Setup, Form, Muscles Worked, and FAQ`}
  h1={topic.title}
  description={topic.description}
  path={topic.path}
  category="Leg Workouts"
  categoryPath="/workouts/leg-workouts/"
  updated="June 2026"
  lead={topic.summary}
  faqs={topic.faqs}
>

  <MedicalNote />

  <ExerciseArticle topic={topic} />
</ContentLayout>
